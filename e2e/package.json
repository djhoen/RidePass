{
  "name": "ridepass-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI smoke tests for RidePass",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen http://acme.ridepass.local:3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
